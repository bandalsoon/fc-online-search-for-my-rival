# ULTIMATE v4.5 안정성 감사 인수인계

기준일: 2026-08-23  
검토 기준 구현: `2aafe668ca54ca4176028994fc750e4b1e0560af`  
상태: **아래 항목은 발견·분류만 했으며 아직 수정하지 않았다.**

이 문서는 현재 사이트가 해킹됐거나 D1이 손상됐다는 사고 보고서가 아니다. 코드 경로에서 앞으로 장애·자원 소진·기록 누락을 만들 수 있는 위험을 다음 안정화 작업자가 안전한 순서로 처리하기 위한 문서다.

## 1. 최우선 원칙

- 안정화 작업에서도 사용자 화면과 v4.5 기능을 임의 변경하지 않는다.
- `.env`, API 키, Secret, 기존 D1 레코드를 읽거나 출력·삭제·초기화하지 않는다.
- D1 구조 변경은 원격 데이터 현황, 방향별 row 수, 복구 방법을 먼저 확인한 뒤 사용자 승인을 받는다.
- 일시적인 외부 장애가 전체 사이트를 불필요하게 죽이지 않게 하되, 데이터 누락을 정상으로 위장하지 않는다.
- 수정마다 가장 작은 회귀 테스트를 추가하고 기존 포메이션 테스트를 유지한다.

## 2. P1 — 공개 Archive 조회의 자원 소진 가능성

위치:

- `worker/index.ts`의 `/api/archive`
- `server/archive.ts`의 pair별 Archive 생성·캐시

현재 위험:

- API route의 `home`, `rival` query를 외부 사용자가 임의로 바꿀 수 있다.
- 캐시되지 않은 쌍 하나는 OUID 2개 조회, 계정별 최대 10,000 IDs, Match Detail, 선수 급여, D1 저장까지 유발할 수 있다.
- Secret 값은 노출되지 않더라도 Nexon 호출량과 Worker/D1 자원이 소진될 수 있다.

권장하는 가장 작은 해결:

- 운영 서비스는 고정 Archive이므로 기본 `새로운성연합`/`피버슛` 조합만 허용한다.
- 역순 요청을 지원할 필요가 있다면 동일 pair로 정규화하고 응답 방향 정책을 명확히 한다.
- 다른 닉네임 검색 기능이 미래 명세에서 필요해질 때 별도 인증·rate limit·작업 큐 정책을 설계한다.

완료 조건:

- 임의 닉네임 pair가 비싼 동기화를 시작하지 못한다.
- 기본 운영 Archive와 정상 새로고침은 유지된다.
- 허용/거부 요청에 대한 작은 서버 또는 Worker 테스트가 있다.

## 3. P1 — D1 오류와 손상 row의 은폐

위치:

- `server/archive.ts`의 store `load/save/count` catch fallback
- `worker/d1-store.ts`의 JSON parse 실패 처리

현재 위험:

- D1 load 실패가 빈 배열처럼 처리될 수 있다.
- save/count 실패가 0 또는 현재 summary 값으로 대체될 수 있다.
- 손상된 `raw_detail` JSON은 조용히 제외된다.
- D1에만 남은 과거 경기가 응답에서 사라져도 10분 Archive 캐시에 불완전 결과가 저장될 수 있다.

권장 정책:

- `loadFailed`, `saveFailed`, `malformedRows` 같은 내부 상태를 분리한다.
- D1 load 실패 시 “DB에 기록이 0개”와 동일하게 취급하지 않는다.
- 불완전 결과를 정상 Archive 캐시에 저장하지 않는다.
- 전체 서비스 실패 또는 API-only 임시 결과 중 어떤 정책을 쓸지 명시하고, 사용자 진단에는 기록 보존 위험을 분명히 표시한다.
- 로그에는 matchId 등 최소 식별정보만 사용하고 Secret/원본 민감값을 출력하지 않는다.

완료 조건:

- D1 장애와 실제 0건을 구분할 수 있다.
- malformed row 수가 진단 가능하다.
- DB 장애 중 생성된 불완전 결과가 정상 캐시로 남지 않는다.
- 정상 D1 요청은 기존 17경기와 저장 동작을 유지한다.

## 4. P2 — `match_id` 전역 PK와 방향성 pair 조회

위치:

- `migrations/0001_create_rivalry_matches.sql`
- `worker/d1-store.ts`

현재 구조:

- 기본키는 전역 `match_id`다.
- 조회는 `home_ouid = ? AND rival_ouid = ?` 방향을 구분한다.
- 충돌 update는 `raw_detail`, `last_verified_at`만 갱신하고 pair columns는 바꾸지 않는다.

가능한 문제:

- 같은 경기가 먼저 B→A로 저장된 뒤 A→B로 조회되면 전역 PK 때문에 새 row가 생기지 않는다.
- 기존 row의 pair 방향도 바뀌지 않아 반대 방향 조회에서 D1 경기 하나가 누락될 수 있다.

중요:

- 현재 사이트는 고정 A→B 방향을 사용하므로 즉시 장애가 확인된 것은 아니다.
- 이미 저장된 D1을 조사하지 않고 migration하거나 pair columns를 일괄 변경하면 안 된다.

설계 후보:

1. pair를 정렬한 canonical key로 저장·조회
2. matchId만으로 먼저 불러온 뒤 두 OUID 참여를 원본 JSON에서 확인
3. 별도 rivalry membership 테이블과 match detail 테이블 분리

다음 작업 전 필수 조사:

- 현재 D1 row 수와 OUID 방향 조합 수
- 동일 matchId에 대한 방향 전환 요청 가능성
- 새 구조로 옮길 때 기존 row를 잃지 않는 migration/rollback 방법

## 5. P2 — 불완전 메타데이터의 24시간 캐시

위치: `server/archive.ts`의 `getMeta`

현재 위험:

- `spid.json`, `seasonid.json`, `spposition.json` 실패를 빈 배열로 바꾼다.
- 빈 배열이 포함된 Meta 전체를 정상처럼 24시간 캐시한다.
- 일시적인 Nexon 장애가 선수 ID, 시즌 누락, `SUB` 위치, 잘못된 포메이션 라벨로 하루 동안 이어질 수 있다.

권장 해결:

- 필수 Match Type 메타데이터와 선택 메타데이터 상태를 구분한다.
- 불완전 Meta를 장기 캐시하지 않는다.
- 마지막 정상 Meta 스냅샷이 있으면 유지하고 짧은 시간 뒤 재시도한다.

## 6. P2 — TTL 캐시의 크기 상한과 정리 부재

위치:

- Match Detail cache
- salary cache
- Archive pair cache

현재 위험:

- TTL 만료 여부는 같은 key가 다시 요청될 때만 확인한다.
- 만료됐지만 재사용되지 않는 key가 Map에 계속 남을 수 있다.
- 임의 pair 요청과 결합하면 장기 Node 프로세스의 메모리 증가와 Worker isolate 교체를 유발할 수 있다.

권장 해결:

- 공개 pair 제한을 먼저 적용한다.
- 각 캐시에 합리적인 최대 개수와 만료 entry 정리 정책을 둔다.
- 진행 중 Promise 병합은 유지한다.
- 상세 캐시는 현재 D1 영구 저장과 역할이 겹치지 않도록 한다.

## 7. P2 — 선수 급여 실패의 반복 재시도

위치: `server/archive.ts`의 salary cache

현재 위험:

- 공식 데이터센터 조회가 실패해 `null`이면 캐시 entry를 즉시 제거한다.
- Archive가 다시 만들어질 때마다 카드당 최대 3회의 15초 POST가 반복될 수 있다.

권장 해결:

- 성공값은 기존 장기 캐시를 유지한다.
- 실패값은 짧은 negative-cache TTL로 기억한다.
- `—` fallback과 검증 스냅샷 우선 정책을 유지한다.

## 8. P2 — 외부 및 D1 데이터 런타임 검증 부족

위치:

- Nexon JSON 응답
- D1 `raw_detail` JSON
- Archive 조립 전 Match Detail 경계

현재 위험:

- TypeScript type assertion은 실행 시 데이터 형식을 검사하지 않는다.
- Nexon 스키마 변경이나 구조가 잘못된 row 하나가 `matchInfo.find` 같은 호출에서 500을 만들 수 있다.

권장 해결:

- 현재 기능에 필요한 최소 필드만 경계에서 검증한다.
- 선택 필드가 없다는 이유로 유효한 경기를 과도하게 버리지 않는다.
- 잘못된 row를 격리하고 진단 수를 반환한다.
- 작은 수동 validator가 더 적은 변경인지, 검증 범위가 커질 때 검증 의존성을 도입할지 실제 범위를 보고 결정한다.

## 9. P2/P3 — CI 검증 공백

### 포메이션 테스트 누락

현재 `.github/workflows/build.yml`은 `pnpm build`를 실행하지만 `pnpm test:formation`을 실행하지 않는다.

위험:

- RF/LS 깊이, 3백/4백 윙백 좌표 회귀가 Build 성공 상태로 main에 들어갈 수 있다.

권장 해결:

- dependency 설치 후 build 전에 `pnpm test:formation`을 추가한다.

### 메타데이터 검증이 assert하지 않음

현재 workflow의 Node 명령은 친선 관련 배열을 출력만 하고 비어 있어도 성공 종료할 수 있다.

권장 해결:

- 현재 필수 모드 이름과 ID가 실제 응답에 존재하지 않으면 non-zero exit 하게 한다.
- 현재 운영 정책인 40/60과 메타데이터 이름을 함께 확인한다.

## 10. P3 — Node production 정적 경로 불일치

현재:

- Vite client output: `dist/client`
- Express production static/sendFile: `dist`, `dist/index.html`

영향:

- `NODE_ENV=production pnpm start`로 SPA까지 제공하려 하면 경로가 맞지 않는다.
- 현재 Cloudflare Worker/Assets 배포에는 영향이 없다.

처리 원칙:

- Node production 배포를 실제 지원할 명세가 생길 때 수정한다.
- 이 문제만을 이유로 정상 Cloudflare 배포 경로를 바꾸지 않는다.

## 11. P3 — 타입·버전·health 계약 중복

현재 서버 응답 타입과 `src/types.ts`, 로컬/Worker health 정보가 별도로 유지된다.

위험:

- v5 필드 변경 시 한쪽만 수정해도 컴파일 단계에서 항상 잡히지 않는다.

처리 원칙:

- 다음 실제 API 계약 변경 시 최소 공용 타입/상수 범위를 검토한다.
- 이 문제만을 위한 대규모 폴더·프레임워크 재구성은 하지 않는다.

## 12. P3 — 미사용 또는 미래용 항목

남아 있는 후보:

- 항상 `null`인 `PlayerRanking.value`
- UI에 직접 표시되지 않는 BEST XI `wins`, `positionScore`

2026-08-23 최소 정리에서 import되지 않던 `zod`와 JSX에 연결되지 않던 `.ranking-grid`는 제거했다.

처리 원칙:

- 공개 API 계약 또는 다음 기능의 예정 필드일 수 있으므로 자동 삭제하지 않는다.
- cleanup 요청이 있을 때 실제 import·응답 소비자·배포 경로를 다시 확인한다.

## 13. 권장 안정화 순서

### 1단계 — DB를 변경하지 않는 저위험 수정

1. 운영 Archive pair 제한
2. CI에 포메이션 테스트 추가
3. 메타데이터 CI를 실제 assert로 변경
4. 불완전 메타데이터 장기 캐시 방지
5. 급여 실패 negative cache
6. cache 최대 크기/정리
7. 외부/D1 최소 런타임 검증

### 2단계 — D1 오류 정책

1. D1 load/save/count 오류 상태 분리
2. malformed row 진단
3. 불완전 결과 Archive 캐시 금지
4. 정상/장애 시나리오 테스트

### 3단계 — 별도 승인 후 D1 방향성 정리

1. 원격 row 현황 읽기 전용 조사
2. canonical pair 설계 확정
3. 새 migration과 rollback 계획
4. 백업·검증 후 적용
5. 기존 경기 수와 원본 JSON 보존 확인

## 14. 안정화 완료 보고 형식

### 완료

- 수정한 위험
- 추가한 테스트
- 기존 17경기/기능 보존 여부
- 운영 반영 여부

### 미완료

- 남긴 위험과 이유
- 별도 승인이 필요한 DB 작업

### 확인 필요

- 실제 운영에서 사용자가 확인해야 할 항목
- 외부 Nexon/Cloudflare 상태 때문에 단정하지 못한 항목

보안상 API 키 값, Secret 값, D1 원본 JSON은 완료 보고에 넣지 않는다.


