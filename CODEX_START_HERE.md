# Codex 작업 시작점

이 저장소에서 작업하는 Codex는 코드를 수정하기 전에 아래 문서를 순서대로 읽는다.

1. `AGENTS.md` — 저장소 공통 작업 규칙
2. `HANDOFF_V4.5.md` — 현재 운영 중인 v4.5 구조와 보호 대상
3. `PROJECT_HISTORY_V1_TO_V4.5.md` — v1부터 v4.5까지 변경·대체·폐기된 결정
4. `V4.5_STABILITY_AUDIT.md` — 아직 수정하지 않은 안정성·보안 위험
5. 사용자가 이번 작업에 제공한 최신 기능명세서와 모든 첨부 이미지

## 기준의 우선순위

요구사항이 충돌하면 다음 순서로 판단한다.

1. 현재 대화에서 사용자가 명시한 최신 요청
2. 사용자가 제공한 가장 최신 버전 기능명세서
3. 현재 운영에서 검증된 코드와 `HANDOFF_V4.5.md`
4. `PROJECT_HISTORY_V1_TO_V4.5.md`의 결정 이력
5. 이전 버전 기능명세서
6. README의 사용자 안내

이전 명세에만 있고 이후 명세에서 변경·삭제된 기능을 되살리지 않는다. 반대로 최신 명세에서 언급하지 않았다는 이유만으로 기존 정상 기능을 삭제하지 않는다.

## 코드를 고치기 전 필수 보고

첨부 이미지가 지정된 작업은 이미지를 먼저 실제로 확인한다. 첫 응답은 다음 순서로 짧게 작성한다.

1. 첨부 이미지별 한 줄 설명
2. 이해한 변경사항
3. 유지할 기존 기능
4. 애매하거나 충돌하는 사항
5. 구현 순서

필수 이미지가 없거나 열 수 없으면 추측하지 말고 사용자에게 다시 요청한다. 이미지가 필요한 UI 작업은 보류한다.

## 항상 보호할 것

- `.env`, `NEXON_API_KEY`, Cloudflare Secret과 실제 비밀값
- 기존 Cloudflare Worker/Assets/D1 배포와 운영 데이터
- 양쪽 OUID 및 Match ID 수집, 페이지네이션, 합집합, 중복 제거, 두 OUID 참여 검증
- 감독모드 제외와 공식 메타데이터 기반 Match Type 판정
- Match Detail 원본 보존, 실패 격리, timeout, 동시성 제한, 캐시
- 경기 상세, 분석, BEST XI, 선수기록, 모바일, 이미지 fallback
- 사용자의 기존 변경사항과 Git 이력

비밀값을 읽거나 출력하지 않는다. DB 삭제·초기화, 기존 migration 덮어쓰기, Secret 재등록, API 키 교체, 강제 Git 이력 변경은 사용자의 명시적 승인 없이 하지 않는다.

## 작업 범위 원칙

- 필요한 부분만 최소 수정한다.
- 명세에 없는 기능과 디자인을 임의 추가하지 않는다.
- 불필요한 폴더 재구성, 프레임워크 교체, 배포 플랫폼 이전을 하지 않는다.
- 외부 API 데이터가 없으면 Mock·추정값으로 채우지 않는다.
- 버그 수정은 실제 원인을 확인하고 관련 호출 경로를 끝까지 읽은 뒤 적용한다.
- 안정성 작업은 `V4.5_STABILITY_AUDIT.md`의 위험도와 데이터 보존 순서를 따른다.
- D1 구조 변경은 기존 데이터 조사와 복구 계획을 먼저 제시한 뒤 별도 승인받는다.

## 완료 기준

일반 기능 변경 후 최소 확인:

```bash
pnpm typecheck
pnpm test:formation
pnpm build
```

그다음 PC·모바일 실제 화면, `/api/health`, `/api/archive`, 경기 수와 D1 진단 수치를 확인한다. 배포가 범위에 포함되면 운영 URL까지 검증한다.

완료 보고는 다음 세 항목으로 작성한다.

- 완료
- 미완료
- 확인 필요

## 현재 기준점

- 운영 버전: `ULTIMATE v4.5`
- 운영 주소: <https://fc-online-rival-archive.rhrjsals0103.workers.dev/>
- v4.5 구현 기준 커밋: `2aafe668ca54ca4176028994fc750e4b1e0560af`
- 현재 `main`에는 구현 커밋 이후 인수인계·프로젝트 지침·Codex 스킬 문서가 추가되어 있다. 작업 시작 시 최신 원격 `main`을 다시 확인한다.

