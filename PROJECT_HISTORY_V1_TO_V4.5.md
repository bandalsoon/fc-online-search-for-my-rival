# FC ONLINE Rival Archive — v1~v4.5 프로젝트 역사와 결정 기록

이 문서는 과거 명세를 통째로 복사한 문서가 아니다. 다음 작업자가 왜 현재 구조가 만들어졌는지, 이전 요구 중 무엇이 유지·변경·폐기됐는지 이해하기 위한 결정 기록이다. 현재 구현 방법은 `HANDOFF_V4.5.md`, 작업 순서는 `CODEX_START_HERE.md`를 우선한다.

## 1. 전체 프로젝트의 변하지 않는 목적

이 프로젝트는 일반 FC ONLINE 전적 검색 서비스가 아니다.

- `새로운성연합`과 `피버슛` 두 사람만을 위한 개인 Rival Archive다.
- 두 사람을 HOME/RIVAL로 구분하되 화면과 통계에서는 동등한 주체로 취급한다.
- Nexon Open API와 이미 검증해 D1에 보관한 실제 데이터만 사용한다.
- API가 반환하지 않는 과거 경기, 과거 닉네임, 과거 OUID를 만들어내지 않는다.
- 사용자는 코드를 직접 수정하지 않는다. Codex가 분석·수정·테스트·배포·보고까지 맡는다.
- `.env`, `NEXON_API_KEY`, Cloudflare Secret은 작업 대상이 아니다.

## 2. 버전 계보 한눈에 보기

### 초기 v1 기반 — 실행 가능한 사이트 만들기

초기 요청은 기존 GitHub 프로젝트를 React + Vite + TypeScript 프론트와 Node.js + TypeScript API 서버로 안정화하고, 사용자가 `NEXON_API_KEY`만 설정하면 한 명령으로 실행할 수 있게 만드는 것이었다.

초기 핵심:

- 기본 라이벌 `새로운성연합 VS 피버슛`
- 닉네임 → OUID → Match ID → Match Detail → 맞대결 필터
- 승무패, 경기 목록, 선수 득점 TOP
- 프론트/백엔드 동시 실행과 Codespaces 대응
- package.json/App 충돌, 컨테이너 리빌드, API key invalid 경험을 고려한 안정화

대표 커밋:

- `06a259871d43` 초기 README
- `f4753452f65e` package.json 초기화
- `3618021b95dd` HTML 구조
- `d875211909c5` React 진입점
- `f1496e70fd96` App 구성
- `cb647ff4d0d9` 완성형 Rival Archive 기반
- `2d474704b804` Codespaces 시작 안정화

### v1.1 — 최근 전적 화면에서 Archive로 전환

v1.1은 “최근 200경기 중 상대전” 수준이던 구조를 API가 반환하는 끝까지 탐색하는 개인 아카이브로 바꾸는 첫 명세였다.

추가·변경:

- 최근 100/200/`scan=200` 제한 제거
- `/user/match` offset 페이지네이션
- 기존 `matchType: 50` 고정 제거와 친선 Match Type 확인
- `matchId` 중복 제거
- 총 경기, 승률, 평균 득점·실점
- HOME 득점왕뿐 아니라 도움왕 추가
- `spId + spGrade` 선수카드 구분
- 시즌, 강화단계, 공식 선수 이미지, TOP 8
- 경기 최신순, 20개씩 더 보기
- timeout, 동시성 제한, 상세 개별 실패 격리, 메타데이터 캐시

당시 보류:

- 과거 닉네임/OUID
- 영구 DB
- HISTORY, 연승·연패, 상세 선수 페이지
- 공식 출처가 없는 실시간 선수 시세

후속 변경:

- v1.1은 주로 HOME 기록 전체 스캔 후 상대를 찾는 흐름이었다. v1.2에서 양쪽 계정 조회로 강화됐다.
- 여러 1대1 Match Type을 넓게 고려하자는 문구는 이후 버전마다 정책이 바뀌었고, 현재는 공식 메타데이터로 확인한 40과 60만 포함한다.

### v1.2 — 양쪽 조회와 FC ONLINE 선수카드 확정

v1.2는 사이트 목적을 “공식경기 전적 사이트가 아닌 두 사람의 친선 역사”로 다시 고정했다.

추가·변경:

- HOME과 RIVAL 양쪽 `/user/match` 전체 조회
- 두 목록 합집합과 `matchId` 중복 제거
- Match Detail에서 두 OUID 동시 참여 재검증
- 17경기 문제를 Match Type, 페이지네이션, 한쪽 조회, 상세 실패 관점에서 실제 조사
- 양쪽 승률·평균 득실과 동등한 VS UI
- 닉네임 선택에 따라 양쪽 득점왕/도움왕 전환
- 공식 페이스온/액션샷, 시즌 이미지와 fallback
- 강화 스프라이트는 공식 리소스가 확인될 때만 사용하고 아니면 CSS 배지
- 메모리 캐시는 허용하되 영구 DB는 아직 도입하지 않음

유지된 금지사항:

- 비공식 이미지 크롤링/핫링크
- 가짜 선수 가격
- API 데이터가 없는 기록 추정

### v1.x ULTIMATE 통합 명세 — 보안·README·검증 원칙 정착

2026-08-16 새벽 ULTIMATE 명세는 v1.1과 v1.2를 하나의 실행 지침으로 통합했다. 별도 번호는 없지만 v2.0 직전의 기준 문서 역할을 했다.

확정한 핵심:

- 양쪽 OUID/Match ID 전체 탐색, 합집합, 중복 제거, 두 OUID 검증
- 서버 진단값: HOME/RIVAL IDs, unique IDs, 상세 성공/실패, 맞대결, 최초/최근 날짜
- 양쪽 선수 통계와 공식 이미지
- `.env` 절대 수정 금지
- `.env.example` 및 유사 템플릿 불필요한 생성 금지
- README를 단계별 코딩 안내가 아니라 완성형 사용 설명서로 변경
- Local/Network와 `0.0.0.0` 유지
- 빌드뿐 아니라 실제 API 데이터 검증과 17경기 원인 보고

당시 DB는 명시적으로 보류됐으며 v4에서 도입됐다.

대표 커밋:

- `c4dad4f9569c` 전체 라이벌 통계 확장
- `ef965ec970c9` CI 서버 health 검증
- `515dd776b2ce` 공식 친선 메타데이터 검증
- `82d49eed52bd` Ultimate 친선 Rival Archive 완성

### v2.0 — API 검증 기반 ULTIMATE 기준

v2.0은 기능 추가보다 “실제 공식 API로 확인되는 범위만 구현”하는 원칙을 강하게 고정했다.

추가·변경:

- 공식 `matchtype.json`과 실제 코드의 Match Type을 실행 시 검증
- 당시 기준 `matchType = 40`, `클래식 1on1` 전용 Archive로 정의
- 계정별 10,000 Match ID 안전상한과 도달 여부 진단
- OUID 기준 인물 판별, 향후 한 사람에 복수 OUID 연결 가능한 구조 고려
- 17경기 원인을 실제 데이터 숫자로 보고
- 2026-07-07 이후 종료된 전역 “모든 매치 목록” 역검색에 기대지 않음
- 양쪽 선수 득점/도움/출전/공격포인트/경기당 공격포인트
- 공식 `spid.json`, `seasonid.json`, 페이스온과 이미지 fallback
- 선수 가치 영역 완전 제외
- Match Detail 실패 Promise 제거와 다음 요청 재시도
- Metadata/Match Detail/Archive 메모리 캐시 정책
- Nexon 갱신 지연과 데이터 보존 범위 안내
- 향후 v3의 별칭/OUID 모델과 영구 Archive DB 방향 제안

당시 보류:

- 자동 과거 닉네임/OUID 발견
- API가 반환하지 않는 옛 경기
- 실시간 시장가
- 전역 경기 DB 검색
- 영구 DB 실제 도입

후속 변경:

- v2.0의 40 단독 정책은 v3의 범위 확장과 사용자의 감독모드 제외 지시를 거쳐, 현재 `클래식 1on1(40)`과 `공식 친선(60)`만 포함하는 정책으로 대체됐다.

### v2.1 — 실제 백엔드·배포·보안까지 완료 조건으로 추가

v2.1은 v2.0 전체를 상속하면서 “프론트 데모가 아니라 실제 서비스”를 완료 기준으로 만들었다.

추가·변경:

- 브라우저 → 백엔드 → Nexon API 구조 강제
- 프론트 번들에 `VITE_NEXON_API_KEY` 같은 키 노출 금지
- 로컬 `.env`는 그대로 두고 배포 환경에서는 Secret 사용
- API 키를 받더라도 먼저 공식 API와 실제 응답 검증
- HOME/RIVAL 페이지 끝, 합집합, 상세 성공/실패, 가장 오래된 날짜를 단계별 조사
- 기억 속 8년 기록과 공개 API 기록 차이를 코드 문제, 과거 OUID, 과거 닉네임, API 보존 한계로 분리
- 기능을 `가능 / 조건부 가능 / 현재 불가능`으로 판정하고 Mock으로 숨기지 않음
- HTTPS, 새로고침, API route, 모바일, 실제 배포 URL 검증
- GitHub/번들/응답/README/로그의 API 키 미노출 확인

배포 결과:

- `77f297d14527`에서 Cloudflare Workers 기반 운영 배포가 구성됐다.
- `6bddaff7e605`에서 Cloudflare pnpm workspace 설치 문제가 해결됐다.
- `e58ae35d3b85`에서 공식 선수 페이스 이미지 fallback이 보완됐다.

### v3.0 — 실서비스 가독성과 Match Type 범위 재정의

v3는 17이라는 숫자 자체를 반복 설명하는 요구를 삭제하고, “현재 API에서 찾을 수 있는 최대 기록”에 집중했다.

추가·변경:

- 17경기 원인 장문 분석보다 HOME/RIVAL 실제 조회 범위 보고를 우선
- v2.0의 40 단독 정책을 폐기하고 가능한 1대1 성격 Match Type 확대 시도
- 공식경기·리그·볼타 제외
- 이후 사용자의 명시적 추가 지시로 감독모드도 완전 제외
- 실제 메타데이터 이름과 두 OUID 참여 여부로 최종 판정
- 여러 타입이 있으면 필터 UI를 제안했으나 v4에서 필터 UI는 제거됨
- 첫 화면을 VS, 총 경기·승무패·승률, 평균 득실 순으로 단순화
- 의미 없는 영문, 과도한 색·glow·진단정보를 축소
- 진단정보는 접을 수 있는 `데이터 정보`로 이동
- Cloudflare Worker 배포 구조 유지
- 사용자용 완료/부분/미구현 보고서 요구

최종 구현 결정:

- 현재는 모든 비공식 1대1 모드를 자동 포함하지 않는다.
- 공식 메타데이터 명칭이 정확한 `클래식 1on1(40)`과 `공식 친선(60)`만 허용한다.
- 감독모드 52, 공식경기, 리그, 볼타, AI, 연습, 훈련과 기타 모드는 제외한다.
- 경기 Match Type 필터는 v4의 결정에 따라 사용자 화면에 없다.

대표 커밋:

- `c2e1a63601cd` 1대1 친선 Archive 확대 및 감독모드 제외

### v4.0 — 경기 상세·BEST XI·분석·영구 DB

v4는 단순 전적 사이트를 경기 하나하나와 선수 역사를 다시 볼 수 있는 Archive로 확장했다.

추가·변경:

- 탭: `개요 / 경기 / 선수 / BEST XI / 분석`
- 경기 클릭 상세 펼침, 펼친 경기만 렌더링
- 실제 Match Detail의 `spPosition` 기반 당시 라인업
- 실제 데이터가 있을 때만 득점 타임라인과 팀별 최고 평점 선수
- 경기 상세 지표를 점유율·슈팅·유효슈팅·패스 네 가지로 제한
- 각 팀 최빈 실제 포지션 구성을 대표 포메이션으로 사용
- BEST XI 내부 Position Score: 출전 안정성 35%, 누적 공격 30%, 경기당 공격 25%, 승리 10%와 역할·표본 보정
- Position Score를 실제 축구 평점처럼 표시하는 행위 금지
- 분석 탭에 라이벌 지표와 Rival Records 통합
- 최대 점수차, 최다 득점 경기, 연승·무패·연패, 최빈 스코어, 선수 단일 경기 기록, 이정표
- Cloudflare D1 영구 DB 도입
- 검증된 Match Detail 원본 JSON 보관, `matchId` 중복 방지, DB 우선 조회
- 메모리 캐시는 속도, D1은 영구 기록의 기준으로 역할 분리
- v3/v4 복구 브랜치 요구

첨부 이미지 A:

- FotMob의 축구장 BEST XI 화면을 참고했다.
- 원형 페이스온, 평점 배지, 골·도움 아이콘, 선수명, 포메이션 간격과 모바일 정보 밀도가 기준이었다.
- 그대로 복제하지 않고 기존 Rival Archive 다크 테마에 맞췄다.

대표 커밋:

- `6aba4b14f345` D1과 BEST XI를 포함한 ULTIMATE v4
- `71a5278cd1c7` 실제 득점 타임라인 필드 처리
- `f3b10880013d` v4 타임라인 배포 확인
- `4e1712879eb3` 공식 도움 선수 ID 해석 보완

### v4.5 — 개요 중심 BEST XI와 고정 좌표·선수 UI 확정

v4.5는 v4를 갈아엎지 않고 BEST XI와 선수 표시를 구체적인 좌표·배치 규칙으로 교정했다.

명시적으로 삭제:

- 독립 `BEST XI` 탭
- 개요의 최근 5경기
- 선수마다 반복되던 `0골 0도움` 텍스트

명시적으로 변경:

- 탭을 `개요 / 경기 / 선수기록 / 분석`으로 변경
- 개요에 두 팀 BEST XI를 동시에 배치
- BEST XI 후보는 통산 10경기 이상 출전한 선수카드만 허용
- 표시 평점은 실제 `spRating` 평균만 사용
- 동일 `spId + spGrade` 카드가 두 자리를 차지하지 않도록 전체 슬롯을 중복 없이 배정
- BEST XI 골·도움은 포지션 구간값이 아니라 해당 카드의 전체 맞대결 통산값 표시
- 내부 후보 평가는 v4 Position Score를 유지하되 이를 화면 평점으로 위장하지 않음
- 중앙 미드필더·측면·중앙 공격형 포지션군 통합
- LS/ST/RS, Wing/CF, CAM, CM, CDM, 수비, GK의 Y축을 고정
- 4백과 3백의 LWB/RWB 위치를 별도 처리
- 선수기록에서 득점왕/도움왕을 한 번에 하나만 표시, 기본값 득점왕
- 한글 UI에 나눔바른고딕 적용

첨부 이미지 1의 선수 UI:

- 상단 중앙 실제 평점
- 아래에 값이 있을 때만 `⚽ 골`과 `👟 도움`
- 원형 배경과 공식 페이스온
- 실제 경기 포지션
- 최하단 `급여 → 시즌 스프라이트 → 선수명 → 강화단계`
- 급여는 시장가가 아니라 선수 카드 급여이며 공식 FC ONLINE 데이터센터 값만 사용

고정 좌표의 최종 핵심:

- LS/ST/RS Y 10
- LW/LF/CF/RF/RW Y 23
- LAM/CAM/RAM Y 35
- LM/LCM/CM/RCM/RM Y 48
- LDM/CDM/RDM Y 61
- 4백 후방선 Y 76
- GK Y 91 중앙
- RF는 오른쪽 X 68, LS는 X 38이며 서로 다른 Y축
- 3백의 LWB/RWB만 Y 61, X 10/90

구현·검증 결과:

- 두 팀 BEST XI 각각 11명, 통산 10경기 조건과 중복 금지 확인
- v4.5 포메이션 회귀 테스트 3개 통과
- TypeScript 검사와 프론트/Worker 빌드 통과
- PC와 390px 모바일에서 탭, BEST XI, 경기 상세, 선수기록 확인
- BEST XI와 경기 라인업의 선수 UI 9요소 확인
- 현재 확인되는 맞대결 17경기, Match Detail 실패 0건
- 공식 페이스온이 없는 선수는 placeholder 사용
- 공식 선수 급여 스냅샷과 신규 카드 공식 조회 fallback 적용

대표 커밋:

- `4813401ca16e` v4.5 개요 BEST XI
- `91b9052adf2d` 공식 데이터센터 급여 조회
- `c13212d8c9d3` 급여 조회 재시도
- `4bba77acbea6` 검증 급여 스냅샷
- `2aafe668ca54` BEST XI 선수명 가독성 보완 — 현재 구현 기준점

## 3. 최종 결정 원장

### 라이벌과 식별자

- 현재 고정 사용자: `새로운성연합`, `피버슛`
- 현재 인물 판별: 닉네임으로 OUID 조회 후 OUID 비교
- 미래 방향: 한 사람에 여러 aliases/OUID 연결
- 자동 과거 닉네임/OUID 발견은 미구현이며 가능한 것으로 가정하지 않는다.

### 경기 범위

- 현재 포함: `클래식 1on1(40)`, `공식 친선(60)`
- 여기서 `공식 친선`은 공식경기 랭크 모드가 아니라 공식 메타데이터의 친선 Match Type 명칭이다.
- 현재 제외: 감독모드 52, 공식경기, 리그, 볼타, AI, 연습, 훈련, 기타 모드
- 공식 메타데이터 이름을 매번 확인하고 두 OUID가 Match Detail에 함께 있어야 한다.
- UI의 Match Type 필터는 v4에서 삭제됐으며 현재 다시 추가하지 않는다.

### 기록 범위와 17경기 결론

- 최근 100/200경기 제한은 제거됐다.
- HOME/RIVAL 양쪽을 끝까지 페이지네이션하고 합집합·중복 제거한다.
- Match Detail 개별 실패 수를 별도 집계한다.
- 전체 경로를 수정·검증한 뒤에도 현재 Archive는 17경기를 확인했고 상세 실패는 0건이었다.
- 따라서 남아 있는 과거 기록 차이는 화면의 20개 표시 제한이나 최근 200경기 제한 때문이 아니다.
- 현재 공개 API와 현재 OUID가 반환하지 않는 과거 기록은 복구할 수 없다. 과거 OUID/닉네임은 가능한 가설이지만 아직 확인되지 않았다.

### 데이터 진실성

- 공식 API/메타데이터/공식 FC ONLINE 리소스만 사용한다.
- 실제 평점이 없으면 평점을 만들지 않는다.
- 득점 시간이 없으면 시간을 만들지 않는다.
- 공식 시장가 데이터가 없으므로 선수 가격을 만들지 않는다.
- 선수 급여는 시장가와 다른 데이터이며 공식 데이터센터 값이 확인될 때만 표시한다.

### 저장

- v1~v3는 메모리 캐시까지만 사용했다.
- v4부터 Cloudflare D1이 영구 기록의 기준이다.
- 검증된 Match Detail 원본 JSON을 `rivalry_matches`에 저장한다.
- 메모리 캐시는 재호출 방지용이며 D1을 대체하지 않는다.
- 기존 D1 데이터는 삭제·초기화하지 않는다.

### 화면 구조

- 현재 탭: `개요 / 경기 / 선수기록 / 분석`
- 개요: 대칭 VS, 핵심 통계, 두 팀 BEST XI
- 경기: 최신순 20개씩, 클릭 상세
- 선수기록: 팀 선택 + 득점왕/도움왕 단일 선택
- 분석: 라이벌 지표 + Rival Records
- 독립 BEST XI 탭, 최근 5경기, Match Type 필터는 현재 없음

### 보안과 실행

- API 키 환경변수는 `NEXON_API_KEY` 하나를 유지한다.
- 로컬은 `.env`, 운영은 Cloudflare Secret을 사용한다.
- 프론트에서 Nexon API를 직접 호출하지 않는다.
- `.env`, `.env.example`, Secret을 기능 작업 중 손대지 않는다.
- 로컬 실행은 프론트/백엔드를 한 명령으로 시작하고 모두 `0.0.0.0` 네트워크 구조를 유지한다.

## 4. 과거에 요구됐지만 현재 되살리면 안 되는 항목

- 초기 `matchType: 50` 고정
- HOME 한쪽의 최근 100/200경기만 검색
- 모든 경기를 `공식 경기`로 표시
- v2.0의 타입 40 단독 고정
- v3의 광범위한 “전체 - 제외목록” Match Type 자동 포함
- 감독모드 포함
- v3 Match Type 필터 UI
- v4 독립 BEST XI 탭
- v4 개요 최근 경기/최근 5경기 영역
- Position Score를 실제 평점처럼 표시
- BEST XI 카드에 포지션별 골·도움만 표시
- 득점왕과 도움왕을 한 화면에 동시에 길게 표시
- `0골 0도움` 반복 표시
- 가짜 선수 시장가, 가짜 평점, 가짜 득점 시간
- API 키를 프론트 환경변수나 코드에 넣기
- 작업 때마다 `.env.example`을 만들거나 `.env`를 덮어쓰기

## 5. 해결된 운영 문제와 교훈

### package/App 및 시작 구조 충돌

- 초기 저장소의 부분 코드와 package 설정 충돌을 완성형 React/TypeScript/Node 구조로 통합했다.
- 사용자가 파일을 하나씩 복사하지 않고 한 명령으로 실행하는 구조가 목표다.

### `.env.example` 혼동

- `.env.example`은 실제 런타임 비밀 파일이 아니다.
- 현재 사용자의 기존 `.env`를 그대로 사용하도록 README를 정리했다.
- 이후 작업자는 example 파일 이름 변경이나 키 재입력을 요구하지 않는다.

### API key invalid

- 키는 백엔드에서만 읽고 Nexon 오류코드를 사용자용 메시지로 변환한다.
- 키 값을 출력하거나 GitHub에 기록하지 않는다.
- 운영에서는 Cloudflare Secret으로 분리했다.

### Codespaces/컨테이너 리빌드

- `2d474704b804`에서 재현 가능한 Codespaces 시작을 보완했다.
- 컨테이너 리빌드 중 연결이 끊겼더라도 원격 GitHub 상태를 먼저 확인하고 완료된 작업을 다시 덮어쓰지 않는 원칙이 생겼다.

### Cloudflare 도입 이유

- 정적 프론트만 호스팅하면 Nexon API 키를 숨긴 백엔드와 영구 저장소가 없다.
- Cloudflare Worker는 API와 정적 자산을 같은 서비스에서 제공하고, Secret과 D1을 연결하기 위해 선택됐다.
- 특별한 이유 없이 다른 배포 플랫폼으로 이전하지 않는다.

### 17경기 문제

- 최근 제한, 한쪽 조회, Match Type, 페이지네이션, 상세 실패를 모두 진단 대상으로 삼았다.
- 현재는 양쪽 전체 조회, 40/60, 중복 제거, 두 OUID 검증, 실패 격리가 적용됐다.
- v4.5 확인 결과 17경기와 상세 실패 0건이므로 남은 과거 기록 부족은 현재 공개 API/OUID 범위의 한계로 취급한다.

### 득점 타임라인과 도움 ID

- v4에서 실제 `shootDetail` 필드를 확인해 득점 시각을 복원했다.
- 도움 선수 ID 필드 차이를 후속 커밋에서 보완했다.
- 데이터가 없는 경기에는 추정값을 만들지 않는다.

### 선수 급여

- Match Detail에 급여가 없어 공식 FC ONLINE 데이터센터 `PlayerAbility`를 검증해 사용했다.
- 현재 등장 카드의 검증값을 스냅샷으로 보관하고 새 카드만 공식 응답을 조회한다.
- 조회 실패는 `—`이며 임의 값으로 채우지 않는다.

## 6. 현재 완료·조건부·미구현

### 완료

- 양쪽 전체 친선 Match ID 조회와 맞대결 검증
- 상대전적과 양쪽 선수 통계
- 경기 목록·상세·당시 라인업·실제 지표
- BEST XI·대표 포메이션·실제 평균 평점
- 분석과 Rival Records
- D1 원본 Match Detail 영구 저장
- Cloudflare 운영 배포, 모바일, 보안, 기본 진단

### 조건부

- 공식 리소스가 존재하는 선수 페이스온/시즌 이미지/급여
- API가 현재 보유한 범위까지의 과거 기록
- 과거 OUID를 사용자가 실제로 확보했을 때의 통합
- Match Detail에 실제 필드가 있는 추가 통계

### 미구현 또는 현재 불가능

- 공개 API가 반환하지 않는 옛 경기 복구
- 과거 닉네임·OUID 자동 발견
- 전역 경기 데이터베이스 역검색
- 공식 근거 없는 실시간 시장가
- 사람/별칭/OUID 영구 모델과 수동 등록 UI
- 정기 증분 동기화·재검증 스케줄러
- 연도별·월별·시즌별 HISTORY 화면

## 7. v4.5 이후 확인된 미해결 위험

2026-08-23 읽기 전용 코드 점검에서 안정성·보안 위험이 발견됐다. 실제 침해나 DB 손상이 발생했다는 뜻은 아니다.

- 공개 `/api/archive`가 임의 닉네임 쌍으로 비싼 조회를 만들 수 있음
- D1 오류와 손상 JSON이 정상 빈 데이터처럼 숨겨질 수 있음
- `match_id` 전역 PK와 방향성 pair 조회의 무결성 간격
- 불완전 메타데이터가 24시간 캐시될 수 있음
- TTL 캐시의 크기 상한/정리 부재
- 급여 실패가 매번 재시도될 수 있음
- 외부/D1 데이터의 런타임 형식 검증 부족
- CI에서 포메이션 테스트와 메타데이터 assert가 merge gate가 아님

수정 순서와 데이터 보존 조건은 `V4.5_STABILITY_AUDIT.md`를 따른다. 특히 D1 pair 구조는 기존 데이터를 조사하기 전 migration하거나 고치지 않는다.

## 8. Git 기준점

- v1 기반 완성: `cb647ff4d0d9`
- v1.x Ultimate: `82d49eed52bd`
- Cloudflare 운영 시작: `77f297d14527`
- v3 범위/감독모드: `c2e1a63601cd`
- v4 D1/BEST XI: `6aba4b14f345`
- v4 타임라인/도움 보완: `71a5278cd1c7`, `f3b10880013d`, `4e1712879eb3`
- v4.5 핵심: `4813401ca16e`
- v4.5 구현 기준: `2aafe668ca54`
- 롤백 브랜치: `rival-archive-v3`, `rival-archive-v4`, `rival-archive-v4.5`

## 9. 다음 버전에서 이 문서를 사용하는 법

1. 최신 기능명세서가 어떤 기존 결정을 명시적으로 변경하는지 찾는다.
2. 변경하지 않은 항목은 현재 v4.5 동작을 유지한다.
3. 과거 명세와 충돌하면 이 문서의 “최종 결정 원장”과 최신 명세를 우선한다.
4. API 데이터가 있는지 먼저 검증하고 가능/조건부/불가능으로 구분한다.
5. 첨부 이미지를 확인하지 못한 UI는 구현하지 않는다.
6. D1·Secret·API 수집 경로를 건드릴 때는 `HANDOFF_V4.5.md`와 안정성 감사 문서를 다시 확인한다.
7. 구현 후 코드·테스트·실제 화면·운영 URL·데이터 수를 함께 검증한다.

