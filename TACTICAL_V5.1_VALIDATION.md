# ULTIMATE v5.1 전술판 검증

기준 main: `7985fcf` (ULTIMATE v5 FINAL), 작업 브랜치: `codex/tactical-v51-20260831`.

## 범위

공통 전술판 계산, 실제 선수 구성 측정, BEST XI 정렬, 관련 테스트/문서만 변경한다. 선수 UI와 데이터 선정/통계/API/DB/환경변수/배포 구조는 유지한다. 운영 배포는 승인 대기다.

## 기준 화면

- 실제 22경기, 전체 경기 상세 44전술판/484명, BEST XI 2전술판/22명.
- 기존 PC BEST XI는 548×880px이며 RIVAL 카드/피치가 12px 아래로 밀려 있었다.
- 기존 390px 화면에서 BEST XI 327×880px, 경기 상세 309×880px로 폭이 달랐다.
- 실제 포지션 구성은 LS+RF형, ST+CAM+LM/RM형, ST+LAM/CAM/RAM형이다. 실제 3백은 없어 테스트 조합으로만 검증한다.

## 자동 검증

- `pnpm typecheck`: 통과.
- `pnpm test:formation`: 14개 통과. 기존 포메이션 의미/표기/MOM/패스 검증 유지, 고정 좌표 기대만 새 모델로 갱신.
- `pnpm build`: 통과 (client/Worker).
- 필수 조합: 4-4-2, 4-2-3-1, 3백+5 DM, LW/RW, LF/RF, 5 CM, LS/ST/RS, LF/CF/RF, 7개 수직 레이어, 실제 기록의 3개 구성.
- PC/390/360/320 가용 폭, 안전 간격/경계/겹침, 좌우/레이어, 동일 배율/양 팀 공통 배율, 결정성, 입력 불변성, 크기 증가 시 재계산을 검증한다.
- 긴 이름/큰 수치/폰트 변경은 더 큰 실제 경계 입력으로 단위 검증하고, 실제 DOM 측정은 별도로 검증한다.

## 실제 화면 검증

최종 검증은 승인 대기다. 수학 테스트만으로 완료 판정하지 않는다.

- 1차 미리보기(5903ec03): 390×844에서 양 팀 309×693px, 배율 1.0, 선수 22명, 누락 0, 겹침 0, 경계 이탈 0, 최소 안전 간격 8px. 기존 정보/폰트 크기 유지.
- 1차 PC 측정(1280×720): 양 팀 520×762px, 카드 588×908px, 헤더 82px 및 카드/피치 상단 일치, 겹침/경계 이탈 0. 이는 최종 1440×900 검증을 대체하지 않는다.
- 1차 화면에서 LS/RF의 Y가 1px 차이여서 같은 레이어처럼 보이는 문제를 발견했다. 최종 코드에서는 인접 의미 레이어 사이의 최소 깊이를 선수 구성 높이의 절반으로 보정했다.
- 1차 320px에서 기존 body 최소 폭 320px과 15px 스크롤바 때문에 문서 가로 넘침을 확인했다. 전술판이 있는 모바일 화면에서만 body 최소 폭을 해제했다. 다른 탭의 스타일은 변경하지 않았다.
- 최종 코드(dc8d8178): Cloudflare 미리보기 배포 성공, 타입 검사/14개 테스트/빌드 통과. 최종 1440×900·390×844·360·320 및 전체 경기 DOM 회귀/스크린샷은 아직 검증 완료가 아니다.
- 반복 가능한 실제 DOM 검사: `tests/pitch-dom-audit.js`의 읽기 전용 함수를 브라우저 evaluate에서 실행한다. 모든 하위 요소/텍스트 경계, 2px 외곽 영역, 안전 간격, 경계 이탈, 선수 7개 상시 요소, 공통 배율/카드 정렬/문서 넘침을 수집한다. 골·도움은 기존 0값 숨김 정책을 별도로 비교한다.

## 검증 중단 사유와 필요한 승인

로컬 브라우저 연결은 보안 정책에 차단되어 우회하지 않았다. 기존 GitHub/Cloudflare 연결의 별도 브랜치 미리보기를 사용했다.

이후 자동 안전 검토가 `/api/archive` 재조회 검증을 거부했다. 이 GET 경로는 단순 DB 읽기가 아니라 경기 수집과 D1 저장/갱신을 수행할 수 있어 사용자의 기존 D1 수정 금지 조건과 충돌한다. 거부 후 재조회 및 페이지 새로고침을 중단했다. 최종 화면 검증을 계속하려면 **기존 사이트의 정상 조회·자동 저장 동작에 대한 명시적 허용**이 필요하다. DB 스키마/레코드 수동 변경이나 Secret 변경 승인을 요청하는 것이 아니다.

별도의 읽기 전용 `/api/health`는 운영/미리보기 모두 ok, apiKeyConfigured, databaseConfigured가 true다. 실제 값은 읽거나 출력하지 않았다. 서버·Worker·migration·타입·MOM/지표 처리·패키지·배포 설정은 main과 차이가 없고, 클라이언트 번들에 키 변수/Authorization 참조가 없다.

미리보기: https://codex-tactical-v51-20260831-fc-online-rival-archive.rhrjsals0103.workers.dev/

Draft PR: https://github.com/bandalsoon/fc-online-search-for-my-rival/pull/5

운영 main은 7985fcf 그대로이며 미리보기 변경을 병합/운영 배포하지 않았다.

## 알려진 확인 필요

좁은 화면의 밀집 행에서는 전체 공통 축소가 필요할 수 있다. 기존 v5 모바일 글자 크기를 임의 변경하지 않으며, 축소 배율/포메이션/화면 폭은 가독성 확인 대상으로 보고한다.

